export const environment = {
  production: false,
  sanity: {
    projectId: 'nznn7r7s',
    dataset: 'production',
    apiVersion: '2025-01-01',
    useCdn: false, // Keep false for development to avoid CORS
    token: '', // Add your Sanity token here if needed
  }
};