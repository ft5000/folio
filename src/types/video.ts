export interface VideoDTO {
    _id: string;
    title: string;
    description: string;
    publishedAt: string;
    videoUrl: string;
}

export interface VideoThumbnailDTO {
    _id: string;
    title: string;
    thumbnailUrl: string;
    publishedAt: string;
}